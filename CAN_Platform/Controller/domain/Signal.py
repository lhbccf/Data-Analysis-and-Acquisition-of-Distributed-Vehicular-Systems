class Signal:
    def __init__(self, signal_id, frame_id, signal_name, value, unit):
        self.signal_id = signal_id
        self.frame_id = frame_id
        self.signal_name = signal_name
        self.value = value
        self.unit = unit